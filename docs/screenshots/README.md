# Screenshots

Add application screenshots here for the project report. The main README links
to these filenames:

- `web-welcome.png` — welcome / login screen (desktop)
- `web-game.png` — gameplay (desktop)
- `mobile-welcome.png` — welcome screen on a narrow viewport
- `mobile-death.png` — death screen / leaderboard on a narrow viewport

Tip: use the browser devtools device toolbar (e.g. iPhone/Pixel preset) to
capture the mobile views — the responsive breakpoints live in `src/style.css`.
